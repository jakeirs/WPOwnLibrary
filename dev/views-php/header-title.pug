include ../views/pug/_mixins
+php(" wp_reset_postdata() ")
.
  <?php
  $args = array(
  'post_type' => 'home_page_content',
  'posts_per_page'    => 1,
  );

  $the_query = new WP_Query( $args );
  var_dump( $the_query);
  $the_query->the_post();

  ?>

+pageHeader({
    bgSrc: "<?php echo get_field( 'first_background2' )['url']; ?>"
}).page-header--adjusted.page-header--nav.jarallax
  .header--large.text--serif.text--900 SequenceAA
  .header.header--medium.text--300.padding--small-tiny-top Tech and IT solutions
  .buttons-container.margin--small-small-top
    .column--button
      +button({
          style: 'primary',
          title: 'Dowiedz się więcej',
          label: 'Dowiedz się więcej',
          rounded: true
      })
    .column--button.margin--medium-default-bottom-down
      +button({
          style: 'transparent',
          title: 'Kup teraz',
          label: 'Kup teraz',
          rounded: true
      }).hollow-border.margin--medium-small-left.margin--small-tiny-left